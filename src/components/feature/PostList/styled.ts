import styled from 'styled-components';

export const PostListWrap = styled.div`
	height: calc(100vh - 14rem);
	position: relative;
	top: 7rem;
	overflow-y: auto;
	.filterDiv {
		height: 0.1rem;
	}
`;
